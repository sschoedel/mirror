add_subdirectory (src)

# set (LIBNAME1 Logic)
# set (SRC
#     src/mirrorgrid.cpp
#     src/tile.cpp
# )
# add_library (${LIBNAME1} ${SRC})
# message (${CMAKE_CURRENT_LIST_DIR})
# target_include_directories (${LIBNAME1} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)