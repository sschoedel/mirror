add_subdirectory (src)

# set (LIBNAME2 Visualization)
# set (SRC
#     src/mirrorvis.cpp
# )
# add_library (${LIBNAME2} ${SRC})
# target_include_directories (${LIBNAME2} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)